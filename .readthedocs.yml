version: 2
build:
  os: ubuntu-22.04
  tools:
    python: "3.10"
submodules:
  include: all
python:
  install:
    - method: pip
      path: .
    - requirements: doc/requirements.txt
  system_packages: true
formats:
  - htmlzip
